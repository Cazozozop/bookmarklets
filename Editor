javascript:(function(){if(document.getElementById('edit-ui'))return;const css="#edit-ui{position:fixed;top:20px;left:20px;background:#111;color:#0f0;font-family:sans-serif;z-index:999999;border:2px solid #0f0;border-radius:8px;padding:10px 10px 10px 40px;box-shadow:0 0 10px #0f08;width:160px;user-select:none;cursor:default}#edit-ui h3{margin:0 0 10px;font-size:16px;pointer-events:none}#edit-ui .close{position:absolute;top:5px;left:5px;color:#0f0;font-weight:bold;font-size:18px;cursor:pointer;user-select:none;z-index:2}#edit-toggle{width:50px;height:28px;background:#222;border-radius:14px;border:2px solid #0f0;position:relative;cursor:pointer;transition:background 0.3s;user-select:none;display:inline-block}#edit-toggle.on{background:#0f0}#edit-toggle .slider{position:absolute;top:2px;left:2px;width:22px;height:22px;background:#111;border-radius:50%;transition:left 0.3s;user-select:none}#edit-toggle.on .slider{left:26px}";const style=document.createElement("style");style.textContent=css;document.head.appendChild(style);const panel=document.createElement("div");panel.id="edit-ui";panel.innerHTML='<div class="close" title="Close">✖</div><h3>Edit Webpage</h3><div id="edit-toggle" title="Toggle Edit Mode"><div class="slider"></div></div>';document.body.appendChild(panel);const closeBtn=panel.querySelector(".close");const toggleBtn=document.getElementById("edit-toggle");closeBtn.onclick=()=>{document.head.removeChild(style);panel.remove();document.body.contentEditable=false};let editing=false;toggleBtn.onclick=()=>{editing=!editing;document.body.contentEditable=editing;panel.contentEditable="false";toggleBtn.classList.toggle("on",editing)};let dragging=false,startX,startY,origX,origY;panel.style.cursor="move";panel.addEventListener("mousedown",e=>{if(e.target===toggleBtn||toggleBtn.contains(e.target)||e.target===closeBtn)return;dragging=true;startX=e.clientX;startY=e.clientY;const r=panel.getBoundingClientRect();origX=r.left;origY=r.top;e.preventDefault()});window.addEventListener("mousemove",e=>{if(!dragging)return;let newX=origX+(e.clientX-startX);let newY=origY+(e.clientY-startY);newX=Math.min(window.innerWidth-panel.offsetWidth,Math.max(0,newX));newY=Math.min(window.innerHeight-panel.offsetHeight,Math.max(0,newY));panel.style.left=newX+"px";panel.style.top=newY+"px"});window.addEventListener("mouseup",()=>{dragging=false})})();
