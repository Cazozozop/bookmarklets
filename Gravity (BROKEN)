javascript:(function(){let els=[...document.querySelectorAll('body *')].filter(e=>{let r=e.getBoundingClientRect();return r.width>0&&r.height>0});els=els.map(el=>({el,area:el.offsetWidth*el.offsetHeight||1})).sort((a,b)=>a.area-b.area);els.forEach(({el},i)=>{el.style.willChange='transform,opacity';el.style.zIndex='9999';el.style.position='relative';setTimeout(()=>{el.style.transition='transform 1s ease, opacity 1s ease';el.style.transform='translateY(200vh) rotate(15deg)';el.style.opacity='0.1'},i*80)});})();
